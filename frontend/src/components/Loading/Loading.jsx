import { LoadingStyles } from "./styles";
import { AiOutlineLoading } from "react-icons/ai";

const Loading = () => {
  return (
    <LoadingStyles>
      <AiOutlineLoading />
    </LoadingStyles>
  );
};

export default Loading;
